<UserControl x:Class="XInput2Key.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XInput2Key.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <CheckBox Grid.Row="0" IsChecked="{Binding IsListening}" Content="Is Listening" />
    <CheckBox Grid.Row="1" IsEnabled="false" IsChecked="{Binding IsConnected1}" Content="Is Connected1" />
    <CheckBox Grid.Row="2" IsEnabled="false" IsChecked="{Binding IsConnected2}" Content="Is Connected2" />
    <CheckBox Grid.Row="3" IsEnabled="false" IsChecked="{Binding IsConnected3}" Content="Is Connected3" />
    <CheckBox Grid.Row="4" IsEnabled="false" IsChecked="{Binding IsConnected4}" Content="Is Connected4" />
  </Grid>
</UserControl>
